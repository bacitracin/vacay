<h2>See all Attractions</h2>

<% @attractions.each do |attraction| %>
  <ul>
    <li><%= link_to attraction.name, attraction_path(attraction) %> <%= attraction.trips.count %> Trips Saved <%= link_to "Delete", attraction, method: :delete, data: { confirm: "Confirm" } %></li>
  </ul>

<% end %>