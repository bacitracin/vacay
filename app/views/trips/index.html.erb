<h2>See Your Trips</h2>

<% @trips.each do |trip| %>
  <li><%= link_to trip.trip_nickname, trip_path(trip) %>  <%= link_to "Delete", trip, method: :delete, data: { confirm: "Confirm"} %></li>
<% end %>
